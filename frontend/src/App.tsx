import React from 'react';

const app = (
  <div>
    <h1>Welcome to Backstage + Temporal Sandbox</h1>
    <p>Basic React app working - Backstage integration next step</p>
  </div>
);

export default app;
